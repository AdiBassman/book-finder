# Optional. BookNook works without this, but a key gives you a private
# per-key daily quota (useful on shared networks). Get one free at:
# https://console.cloud.google.com/  ->  enable "Books API"  ->  create API key
# Then copy this file to `.env` and paste your key below.
VITE_GOOGLE_BOOKS_KEY=
